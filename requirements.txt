# Supported interpreter: Python 3.11.9 (64-bit)
# Install with: py -3.11 -m pip install -r requirements.txt

# Desktop application: data, model inference, reports, and UI charts
numpy>=1.26
pandas>=2.1
scikit-learn>=1.3
joblib>=1.3
matplotlib>=3.8
seaborn>=0.13
shap>=0.44
tkinterdnd2>=0.3

# PCAP fallback used when CICFlowMeter v4 is not installed
cicflowmeter>=0.2
scapy>=2.7

# Narration and the cited-source corpus
# requests talks to the local Ollama server; pypdf re-extracts a PDF when the
# extracted-text cache is absent, which is what source_guard verifies quotes
# against. Both are required, not optional: without them panel 3 falls back and
# every quoted passage fails verification.
requests>=2.31
pypdf>=4.0

# Development notebooks: data ingestion and model training/evaluation
pyarrow>=14
optuna>=3.5
imbalanced-learn>=0.12
interpret>=0.5
lightgbm>=4.0
xgboost>=2.0
catboost>=1.2
gdown>=5.0
ipykernel>=6.0


